<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Botón clasico -->
    <style name="ButtonStyle" parent="Widget.Material3.Button">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_columnWeight">1</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">normal</item>
        <item name="android:backgroundTint">@color/buttonOperator</item>
        <item name="android:textColor">@color/buttonOperatorText</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:insetLeft">0dp</item>
        <item name="android:insetRight">0dp</item>
        <item name="android:padding">0dp</item>
        <item name="cornerRadius">4dp</item>
        <item name="android:gravity">center</item>
    </style>

    <!-- Botón base -->
    <style name="CalcButtonStyle" parent="Widget.Material3.Button">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_columnWeight">1</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/buttonNumberText</item>
        <item name="android:backgroundTint">@color/buttonNumber</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:insetLeft">0dp</item>
        <item name="android:insetRight">0dp</item>
        <item name="android:padding">0dp</item>
        <item name="cornerRadius">8dp</item>
        <item name="android:gravity">center</item>
    </style>

    <!-- Botones especiales: C, DEL -->
    <style name="CalcButtonStyle.C" parent="CalcButtonStyle">
        <item name="android:backgroundTint">@color/buttonC</item>
        <item name="android:textColor">@color/buttonCText</item>
    </style>

    <style name="CalcButtonStyle.Del" parent="CalcButtonStyle">
        <item name="android:backgroundTint">@color/buttonDel</item>
        <item name="android:textColor">@color/buttonDelText</item>
    </style>

    <!-- Botones de operadores: +, -, /, * -->
    <style name="CalcButtonStyle.Operator" parent="CalcButtonStyle">
        <item name="android:backgroundTint">@color/buttonOperator</item>
        <item name="android:textColor">@color/buttonOperatorText</item>
    </style>

    <!-- Botón igual: = -->
    <style name="CalcButtonStyle.Equals" parent="CalcButtonStyle">
        <item name="android:backgroundTint">@color/buttonEquals</item>
        <item name="android:textColor">@color/buttonEqualsText</item>
    </style>

</resources>
